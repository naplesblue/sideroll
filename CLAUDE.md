# CLAUDE.md

完整的项目规范、架构、任务清单和验收标准在 [`AGENTS.md`](./AGENTS.md)。**接手前请先读它。**

## 快速定位

- §1–§4：项目背景、已锁决策、环境、架构
- §5：详细任务清单（带状态、依赖、验收标准、坑）
- §6：端到端验证协议
- §8：**当前进度 / 接手须知**（每次切换 AI 必读）

## 工作约定

- 改动前确认任务在 `AGENTS.md` §5 中是 `TODO` 状态，开始时改成 `IN_PROGRESS`
- 完成时改成 `DONE` 并填日期 + commit hash（如已 git init）
- 同时更新 §8 整体进度表
- 用户已锁定的决策（§2）未经同意不要改
